@using OnlineShop.Web.Areas.Identity
@{ ViewData["Title"] = "خانه"; }

<div class="alert alert-success mt-5">
    <a href="#" class="close" data-dismiss="alert">×</a>
    farhad

    <a class="nav-item nav-link" asp-area="@AreaConstants.IdentityArea" asp-controller="Part" asp-action="Index">
        <span class="fas fa-plug" aria-hidden="true"></span>
        قطعه
    </a>

    <a class="nav-item nav-link" asp-controller="Product" asp-action="Index">
        <span class="fas fa-plug" aria-hidden="true"></span>
        محصولات
    </a>
</div>

<br />
<br />

<p><strong>AjaxOnly</strong> Form</p>
@using (Html.AjaxBeginForm("AjaxMethod", new AjaxOptions
{
    HttpMethod = "post",
    OnBegin = "OnBegin",
    OnComplete = "OnComplete",
    OnSuccess = "OnSuccess",
    UpdateTargetId = "UpdateTargetId"
}))
{
<div class="input-group">
    @Html.AjaxActionLink("حذف", "Remove", "Contoller", new { area = "" }, null)

    @Html.TextBox("name", null, new { placeholder = "Type your name", @class = "form-control mx-sm-2" })
    <button class="btn btn-primary">Submit</button>
</div>}

<script>

    //toastr.options = {
    //    "closeButton": true,
    //    "debug": false,
    //    "newestOnTop": false,
    //    "progressBar": true,
    //    "positionClass": "toast-bottom-right",
    //    "preventDuplicates": false,
    //    "onclick": null,
    //    "showDuration": "300",
    //    "hideDuration": "1000",
    //    "timeOut": "60000",
    //    "extendedTimeOut": "60000",
    //    "showEasing": "swing",
    //    "hideEasing": "linear",
    //    "showMethod": "fadeIn",
    //    "hideMethod": "fadeOut",
    //    "rtl": true
    //};

    function OnBegin() {
        toastr.options.closeButton = true;
        toastr.options.showDuration = 300;
        toastr.options.progressBar = true;
        toastr.options.rtl = false;
        toastr.error('Hi! I am error OnBegin.');
    }
    function OnComplete() {
        toastr.error('Hi! I am error OnComplete.');
    }
    function OnSuccess(data) {
        debugger;
        toastr.options.closeButton = true;
        toastr.options.showDuration = 300;
        toastr.options.progressBar = true;
        toastr.options.rtl = false;
        console.log(data);
        toastr.error('شسحینشسیخج');
    }
</script>